// Retired — the live strip now renders as the second row of `components/Nav.tsx`,
// inside the same fixed container so the two rows stay glued together regardless
// of nav height changes. Safe to delete this file.

export function LiveStrip() {
  return null;
}
